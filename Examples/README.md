# Examples

This directory contains example applications for ObservationTrackingBackfill.

Open `ObservationTrackingBackfill.xcworkspace` at the repository root to work with the package and examples together, or open an example project directly.

- `ObservationTrackingBackfillDemo`: A UIKit grocery list app using manual layout and observable model state.
